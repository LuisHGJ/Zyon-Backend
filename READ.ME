Para rodar a API é necessário primeiro estar rodando o container docker do MySQL.
Na pasta backend (raíz do projeto), rode:

docker compose up -d

Depois, ainda na raíz, é só rodar a API com o comando:

./mvnw spring-boot:run